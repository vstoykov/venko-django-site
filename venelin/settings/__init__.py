"""
Settings Module

If you just set `DJANGO_SETTINGS_MODULE=venelin.settings` you will get an error.

You need to set `DJANGO_SETTINGS_MODULE` to be one of:

- `venelin.settings.dev`
- `venelin.settings.production`
- `venelin.settings.staging`

Or just to set `DJANGO_ENV` to one of `dev`, `production`, `staging`

"""
